export default function Home() {
  return (
    <main>
      <h1>DDx BFF (dashboard-data)</h1>
      <p>
        This package exposes <code>/api/*</code> only. See{' '}
        <code>../docs/BACKEND_HANDOFF.md</code> in the DDx repo (or your team handoff) for the route
        list.
      </p>
    </main>
  );
}
